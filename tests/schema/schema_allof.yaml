$schema: "http://json-schema.org/draft-07/schema#"
$id: "http://www.syrocon.de/allof.schema.json"
title: allOf
description: "allOf demo"
type: object
allOf:
  - properties:
      a:
        type: string
    # additionalProperties: false # doesn't work
  - properties:
      b:
        type: number
